Fun main() {
    // Standard variable
    Ent x = 5;
    
    // Declare BigGraph (note: may need to be treated as an array)
    Arreglo Ent bg1[10];
    
    // Use BigGraph operations
    bg1:nod("A");
    
    Ret 0;
} 